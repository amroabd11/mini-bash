### Shell
+  The project is about to mimic the shell behavior based on bash logic, ```make && ./minishell```
### Futures
+  Expansion
+  Redirection
+  builtins ```cd``` ,```env```,```export```,```unset```,```exit```,```pwd```,```echo```
